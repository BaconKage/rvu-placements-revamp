import "./Atmosphere.css";

// A single faint paper grain. No custom cursor — this is a document, not a toy.
export default function Atmosphere() {
  return <div className="grain" aria-hidden="true" />;
}
